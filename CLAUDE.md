# 项目说明

## Git 规则

- **拉代码**：从 GitLab（remote: `origin`）拉取
  ```
  git pull origin master
  ```

- **推代码**：每次修改后同步推送到两个远程
  ```
  git push github master   # 个人 GitHub（854875058）
  git push origin master   # 项目 GitLab
  ```

- GitHub 需要代理：`127.0.0.1:7897`
