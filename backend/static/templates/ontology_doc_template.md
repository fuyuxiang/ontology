# 本体建模业务文档模板

> 说明：请按照以下六章结构梳理业务信息，AI 将据此提取本体模型中的实体、属性、关系和规则。请将括号内的提示替换为真实业务内容。

---

## 一、业务场景概述

描述本次建模的业务背景、目标和范围。

| 项目 | 内容 |
|------|------|
| 场景名称 | （填写） |
| 业务目标 | （填写） |
| 涉及系统 | （填写） |
| 核心痛点 | （填写） |
| 适用范围 | （填写） |

---

## 二、业务流程

描述端到端业务流程，明确每个环节的角色、动作和状态流转。

### 流程描述

```text
步骤序号. [角色] 执行动作 → 产生结果或状态变更
```

### 状态流转

```text
状态A → 触发条件 → 状态B → 触发条件 → 状态C
```

---

## 三、业务对象与属性

列出场景中的核心业务对象及其关键属性。

| 对象名称 | 属性名称 | 属性类型 | 对应数据表.字段 | 说明 |
|----------|----------|----------|----------------|------|
| （填写） | （填写） | string/integer/number/date/boolean | schema.table.column | （填写） |

---

## 四、对象关系

描述对象之间的业务关联。

| 源对象 | 关系名称 | 目标对象 | 基数 | 关联条件/说明 |
|--------|----------|----------|------|--------------|
| （填写） | （填写） | （填写） | 1:1 / 1:N / N:N | （填写） |

---

## 五、业务规则与动作

描述业务判定规则、触发条件和执行动作。

| 规则名称 | 触发条件 | 执行动作 | 判定逻辑/计算公式 |
|----------|----------|----------|------------------|
| （填写） | （填写） | （填写） | （填写） |

---

## 六、数据源说明

列出本场景涉及的数据源、数据表及其基本信息。

| 数据库/数据源 | 类型 | 包含的主要表 | 更新频率 | 数据质量备注 |
|--------------|------|-------------|----------|-------------|
| （填写） | MySQL/Oracle/PostgreSQL/API | （填写） | 实时/T+1/批量 | （填写） |

---

## 填写建议

1. 对象和属性描述得越完整，AI 抽取越准确。
2. 明确关系基数，避免产生歧义。
3. 规则应写清触发条件和判定逻辑。
4. 属性尽量对应到具体的数据表字段。
5. 暂无内容的章节可以留空，AI 会继续询问缺失信息。
